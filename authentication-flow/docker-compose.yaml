version: '3'

services:

  fc3-keycloak-node-authentication-flow-app:
    build: .
    ports:
      - 3000:3000
    volumes:
      - .:/home/node/app
    extra_hosts:
      - "host.docker.internal:172.17.0.1"
  
networks:
  default:
    external:
      name: fc3-network